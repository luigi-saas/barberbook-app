{
  "name": "@repo/cms",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "basehub dev",
    "build": "if [ -n \"$BASEHUB_TOKEN\" ] && basehub build; then echo '[cms] types regenerated from BaseHub'; else echo '[cms] using committed stub types (no token or regeneration failed)'; fi",
    "analyze": "basehub",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@t3-oss/env-nextjs": "^0.13.10",
    "basehub": "^9.5.3",
    "react": "19.2.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/node": "25.3.5",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "next": "16.1.6"
  }
}
